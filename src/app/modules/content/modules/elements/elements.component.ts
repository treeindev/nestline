import { Component } from '@angular/core';

@Component({
    selector: 'app-elements-components',
    template: `<router-outlet></router-outlet>`,
})
export class ElementsComponent {
    // This is an empty component that will wrapp the elements module.
}
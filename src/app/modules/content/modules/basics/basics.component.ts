import { Component } from '@angular/core';

@Component({
    selector: 'app-basics-components',
    template: `<router-outlet></router-outlet>`,
})
export class BasicsComponent {
    // This is an empty component that will wrapp the basics module.
}
import { Component } from '@angular/core';

@Component({
    selector: 'app-layout-components',
    template: `<router-outlet></router-outlet>`,
})
export class LayoutComponent {
    // This is an empty component that will wrapp the layout module.
}